Tiny video filtering application based on "pipes and filters".

## Installation
Clone the repository and open in any code editor configured with Python 3.4+

Install OpenCV:
```
pip install opencv-python
```

Run `main.py`. Done!

## Multiprocessing
Please note the `multiprocessed` branch